<?php

function demo_module_theme($existing, $type, $theme, $path)
{
    return array(
        'demo_list' => array(
            'variables' => array('items' => array(), 'title' => '')
        )
    );
    
}
